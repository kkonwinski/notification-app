{% extends 'base.html.twig' %}

{% block body %}
    <h1>Dodaj nową osobę</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Zapisz</button>
    {{ form_end(form) }}
{% endblock %}
